import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks'
import { Padding } from './'

<Meta title='Components/Padding/Intro' component={Padding} />

# Padding

---

This component is build to provide a simple way to apply padding

## Example

This example is adding a padding of 1rem on all directions

```jsx
<Padding all={1}>
  <span>Padding content</span>
</Padding>
```

<Canvas>
  <Story id='components-padding--padding-all' />
</Canvas>

## Symetric

```jsx
<Padding horizontal={1} vertical={0.5}>
  <span>Padding content</span>
</Padding>
```

<Canvas>
  <Story id='components-padding--padding-symetric' />
</Canvas>

## Specific directions

```jsx
<Padding top={1} left={2} right={3} bottom={4}>
  <span>Padding content</span>
</Padding>
```

<Canvas>
  <Story id='components-padding--padding-edge-inset' />
</Canvas>
