import React, { CSSProperties, useMemo } from 'react'

// import styled, { css } from 'styled-components'
import { FlexComponent, FlexProps } from './types'

export const Flex: FlexComponent = () => null
