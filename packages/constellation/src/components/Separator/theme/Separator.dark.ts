import { colors } from '../../../colors'
import { SeparatorTheme } from './Separator.theme'

const separatorDarkTheme: SeparatorTheme = {
  colors: {
    default: colors.dark800,
  },
}

export { separatorDarkTheme }
