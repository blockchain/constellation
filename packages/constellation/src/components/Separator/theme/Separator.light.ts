import { colors } from '../../../colors'
import { SeparatorTheme } from './Separator.theme'

const separatorLightTheme: SeparatorTheme = {
  colors: {
    default: colors.grey050,
  },
}

export { separatorLightTheme }
