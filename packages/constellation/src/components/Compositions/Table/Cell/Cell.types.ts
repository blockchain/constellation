import { FC, ReactNode } from 'react'

type BaseCellProps = {}
type BaseCellComponent = FC<BaseCellProps>

type TextCellProps = {}
type TextCellComponent = FC<TextCellProps>

export type { BaseCellComponent, BaseCellProps, TextCellComponent, TextCellProps }
