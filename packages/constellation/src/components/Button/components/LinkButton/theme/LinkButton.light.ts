import { colors } from '../../../../../colors'
import { LinkButtonTheme } from './LinkButton.theme'

const linkLightButtonTheme: LinkButtonTheme = {
  active: {
    color: colors.blue900,
  },
  color: colors.blue600,

  disabled: {
    color: colors.blue200,
  },

  focused: {
    color: colors.blue800,
  },

  hover: {
    color: colors.blue700,
  },

  overlay: {
    color: colors.blue600,
  },
}

export { linkLightButtonTheme }
