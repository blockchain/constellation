import { BaseButtonProps } from 'components/Button/Button.types'
import { FC } from 'react'

type MinimalButtonProps = BaseButtonProps

type MinimalButtonComponent = FC<MinimalButtonProps>

export type { MinimalButtonComponent, MinimalButtonProps }
