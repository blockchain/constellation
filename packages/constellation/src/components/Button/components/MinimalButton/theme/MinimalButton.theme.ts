type MinimalButtonTheme = {
  active: {
    backgroundColor: string
    borderColor: string
  }
  backgroundColor: string
  borderColor: string
  borderRadius: number
  color: string

  disabled: {
    backgroundColor: string
    borderColor: string
    color: string
  }

  focused: {
    backgroundColor: string
    borderColor: string
  }

  hover: {
    backgroundColor: string
    borderColor: string
  }

  overlay: {
    backgroundColor: string
    borderColor: string
    color: string
  }
}

export type { MinimalButtonTheme }
